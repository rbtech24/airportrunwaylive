---
interface Props {
  size?: number;
}
const { size = 40 } = Astro.props;
---
<img
  class="arl-logo"
  src="/images/logo-mark.png"
  width={size}
  height={size}
  style={`width:${size}px;height:${size}px;max-width:${size}px;max-height:${size}px`}
  alt="Airport Runways Live"
/>
